interface:
  display_name: "ATLAS ERP"
  short_description: "Repo-specific patterns and workflows for ATLAS-ERP"
  default_prompt: "Use the ATLAS-ERP repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true